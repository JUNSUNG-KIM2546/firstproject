{{>layouts/header}}

<table class="table">
    <thead>
        <tr>
            <th scope="co1"> No. </th>
            <th scope="co1"> name </th>
            <th scope="co1"> price </th>
        </tr>
    </thead>
    <tbody>
        {{#coffeeShow}}    <!--모델에 담은 객체를 이런식으로 범위를 지정해야 한다.-->
        <tr>
            <th scope="row"> {{id}} </th>
            <td colspan="2"> {{name}} </td>
            <td> {{price}} </td>
        </tr>
        {{/coffeeShow}}
    </tbody>
</table>

{{>layouts/footer}}
